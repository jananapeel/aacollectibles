<section class="page-head">
  <div class="wrap">
    <nav class="crumbs">
      <a href="{{ routes.root_url }}">Home</a><span>/</span>
      <b style="color:var(--mist)">{{ page.title }}</b>
    </nav>
    <div class="page-title">
      <div><h1>{{ page.title }}</h1></div>
    </div>
  </div>
</section>

<section class="sec">
  <div class="wrap">
    <div class="rte page-body">{{ page.content }}</div>
  </div>
</section>
