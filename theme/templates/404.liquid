<section class="sec">
  <div class="wrap" style="text-align:center;max-width:620px">
    <p class="eyebrow" style="justify-content:center">404</p>
    <h1 style="font-family:var(--display);font-size:clamp(30px,5vw,46px);letter-spacing:-.03em;margin-top:16px">
      That one's not in the case
    </h1>
    <p style="margin-top:16px">
      The page you're after has moved or sold. Try a search, or start from the full catalogue.
    </p>
    <div class="cta-row" style="justify-content:center">
      <a class="btn btn-gold" href="{{ routes.all_products_collection_url }}">Browse everything</a>
      <a class="btn btn-line" href="{{ routes.search_url }}">Search</a>
    </div>
  </div>
</section>
